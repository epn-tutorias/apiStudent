extends layout

block header
  h1= title

block content
  
  p Que quieres hacer #{user[0].Name}?
  if(user[0].Rol == 'tutor')
    a(href="/listStudents") Lista de estudiantes
  if(user[0].Rol == 'user' || user[0].Rol == 'admin')
    a(href="/listStudents") Lista de estudiantes
    br
    a(href="/listTutors") Lista de tutores
    br
    a(href="/listUsers") Lista de usuarios
    br
    a(href="/listNotes") Lista de notas
  if(user[0].Rol == 'student' || user[0].Rol == 'graduate')
    br
    a(href="/listNotes/" + user[0]._id.toString()) Lista de notas
    br
    a(href='/createInfoPasan') Si eres pasante por favor llena estos datos.  
    br
  if(user[0].Rol == 'professional')
    a(href='/createQuiz') Encuesta, SOLO PARA GRADUADOS!!!!
block sidebar
  h1
  p
    | Menu
  li
    a(href='/createStudent') Crear cuenta estudiante
  li
    a(href='/createTutor') Crear cuenta tutor
  
  
  